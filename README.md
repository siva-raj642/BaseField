# BaseField
This project uses a static HTML form to collect user input and sends it to a Node.js backend. The backend processes the data and uses EJS Server-Side Rendering to generate a dynamic HTML page, which is then sent back and displayed to the user.
